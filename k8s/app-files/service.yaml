apiVersion: v1
kind: Service
metadata:
  name: node-docker-app
  labels:
    app: node-docker-app
spec:
  type: NodePort
  selector:
    app: node-docker-app
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30080
